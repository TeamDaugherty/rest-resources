{
  "name": "rest-resources",
  "private": true,
  "scripts": {
    "publish": "lerna run build && lerna publish",
    "test": "jest --config=jest.config.js",
    "lerna": "lerna",
    "docs:start": "cd website && npm start",
    "docs:install": "cd website && npm ci",
    "docs:publish": "cd website && npm run publish-gh-pages"
  },
  "devDependencies": {
    "@types/jest": "25.1.3",
    "@types/node": "10.17.17",
    "jest": "25.1.0",
    "lerna": "3.20.2",
    "ts-jest": "25.2.1",
    "tslint": "6.0.0",
    "tslint-eslint-rules": "5.4.0",
    "typescript": "3.8.3"
  }
}
